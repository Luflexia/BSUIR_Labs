1.
2.
-- сложения (ADD, с переносом ADC+флаг CF)
-- команды вычитания (SUB, с переносом SBB-флаг CF)
-- команды умножения (MUL, IMUL+-)
-- команды деления (DIV, IDIV+-) -> рез AL, АХ \ остаток АН, DX
3.

4. 
AND, OR, XOR, NOT

5.
Тип NEAR FAR
Вызывается CALL, при встрече RET возвращается к вызвавшей 
